"use strict";

describe("A suite is just a sunction", () => {
  let a: any;

  it("and so is a spec", () => {
    a = true;

    expect(a).toBe(true);
  });

  it("and so is a specarrrr", () => {
    a = true;

    expect(a).toBe(true);
  });
});
